export interface Product {
  name: string;
  category: string;
  description: string;
  benefits: string[];
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  building: string;
}

export interface ClientItem {
  name: string;
  location: string;
  quote: string;
}
